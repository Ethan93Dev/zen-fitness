export type authType = {
  username: string;
  email: string;
  password: string;
};

export type profileType = {
  firstname: string;
  lastname: string;
  address: string;
  city: string;
  state: string;
  phone: number;
};
